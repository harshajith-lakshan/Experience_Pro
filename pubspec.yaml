name: walletiq
description: WalletIQ — Phase 1 scaffold
publish_to: "none"
version: 0.1.0+1
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
  easy_localization: ^3.0.1
  firebase_core: ^2.2.0
  firebase_auth: ^4.1.0
  google_sign_in: ^6.1.0
  riverpod: ^2.0.0
  flutter_riverpod: ^2.0.0
  go_router: ^7.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.0
flutter:
  uses-material-design: true
  assets:
    - assets/translations/
